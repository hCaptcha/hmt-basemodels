#!/bin/sh
set -exu

FILES="*.py basemodels/*.py"

if [ -z ${CI+x} ]; then
	yapf --style yapf.cfg --i $FILES 
	mypy $FILES --ignore-missing-imports
else
	yapf --style yapf.cfg --diff $FILES
	mypy $FILES --ignore-missing-imports
fi
